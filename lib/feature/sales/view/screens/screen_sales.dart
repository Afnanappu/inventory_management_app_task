import 'package:flutter/material.dart';

class ScreenSales extends StatelessWidget {
  const ScreenSales({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("💰 Sales Screen", style: TextStyle(fontSize: 20)),
      ),
    );
  }
}
